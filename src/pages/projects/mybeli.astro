---
import { getEntry, render } from "astro:content";
import { projects } from "../../data/projects";
import ProjectLayout from "../../layouts/ProjectLayout.astro";

const project = projects.find((entry) => entry.slug === "mybeli");

if (!project) {
  throw new Error("Missing project data for mybeli");
}

const writeup = await getEntry("pages", "mybeli");
if (!writeup) {
  throw new Error("Missing src/content/pages/mybeli.md");
}
const { Content } = await render(writeup);
---

<ProjectLayout project={project}>
  <Content />
</ProjectLayout>
